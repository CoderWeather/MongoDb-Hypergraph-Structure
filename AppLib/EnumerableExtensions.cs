using System.Collections.Generic;

namespace AppLib
{
    public static class EnumerableExtensions
    {
        public static bool Equals<T>(this IEnumerable<T> source, IEnumerable<T> other)
        {
            var result = false;
            
            

            return result;
        }
    }
}